name: dartcodeai
description: DartCodeAI ML/Op - Production ML operations toolkit for embeddings, quota enforcement, and analytics
version: 1.0.0

environment:
  sdk: '>=2.19.0 <4.0.0'

# No external dependencies needed - using dart:io for HTTP requests
